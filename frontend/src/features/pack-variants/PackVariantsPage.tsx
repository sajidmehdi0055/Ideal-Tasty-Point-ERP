import { PlaceholderPage } from '../shared/PlaceholderPage';

export function PackVariantsPage() {
  return (
    <PlaceholderPage
      title="Pack Variants"
      description="Pack Variant (item + brand + pack UOM + conversion factor) ships on feat/inv-s02-uom-brand-pack, awaiting independent review. This screen will list/create/edit pack variants once that branch merges to main."
    />
  );
}
