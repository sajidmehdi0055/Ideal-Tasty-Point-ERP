import { PlaceholderPage } from '../shared/PlaceholderPage';

export function PackVariantsPage() {
  return (
    <PlaceholderPage
      title="Pack Variants"
      description="Pack Variant's backend (item + brand + pack UOM + conversion factor) is live on main (S-02). This list/create/edit screen isn't wired up yet — frontend integration is pending."
    />
  );
}
